# Demo scripts (removed)

Standalone demo seed scripts and synthetic prompt batches used for hackathon walkthroughs lived here. They have been removed so the repo reflects production-style operation: create accounts via `SENTINEL_INITIAL_ADMIN_*` in `backend/.env`, add employees through your connectors, and drive analysis through the API or the browser extension.
